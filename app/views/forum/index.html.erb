<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>

<h2>Forum: 15-minute webboard showcase</h2>
<p>This is a demo of building a webboard in 15 minutes using Rails 3.</p>

<table class="forum">
  <tr>
    <th></th>
    <th>Post / Title</th>
    <th>Replies</th>
    <th>Post by</th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><div class="big_letter">?</div></td>
    <td>      
      <%= link_to post do %>
        <strong><%= post.title %></strong>
      <% end %><br />
      <%= truncate strip_tags(post.body), :length => 60 %>
    </td>
    <td>Replies: <%= post.comments.count %></td>
    <td></td>
  </tr>
<% end %>
</table>

<p><%= link_to '+ Post New', new_post_path %></p>
